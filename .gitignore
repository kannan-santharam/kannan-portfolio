# Environment & Secret Files (NEVER COMMIT API KEYS)
.env
.env.local
.env.*.local
*.env
*.local

# Dependencies
node_modules

# Production Build Output
dist
dist-ssr

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor & OS files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.docx

# Local resume generators (build the PDF/DOCX from src/data/resumeContent.json)
scripts/

# Photo sources for the CV builder; the site only needs the generated .jpg
public/kannan_photo.png
public/kannancrop.png

# Per-company tailored resumes (built by the tailor-resume skill) and local Claude config
companies/
.claude/
